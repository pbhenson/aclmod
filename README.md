aclmod was a utility to manipulate DFS ACLs. It was basically a replacement for
chmod in the DFS environment that supports extended ACL syntax and permissions,
as well as wildcards and recursion. While no longer of any practical use, it
might be of historical interest.
